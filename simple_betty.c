#include "shell.h"

/**
 * the_alpha - Prints the alphabet
 * @i: The character to input
 *
 * Return:1 on success, 0 otherwise
 **/

int the_alpha(int i)
{
	if ((i >= 'a' && i <= 'z') || (i >= 'A' && i <= 'Z'))
	return (1);

	else

	return (0);
}
